# AGENT.md — Project Entry Point

This file is a pointer. The authoritative agent contract is `AGENTS.md` (build, test, publish, conventions for `@mbsks/rspfx-*`).

Documentation standards (tier taxonomy, writing rules, word budgets, slop checklist) live in `docs/AGENTS.md`.

Rationale for non-trivial changes lives in `.agents/notes/implemented/{class}/YYYY-MM-DD-slug.md`; format and lifecycle are defined in `.agents/notes/README.md`.

For documentation changes: locate the fact's single home first, classify reference vs tutorial, apply the writing rules in `docs/AGENTS.md`, co-commit an Agent Note, and verify links and word budgets.
