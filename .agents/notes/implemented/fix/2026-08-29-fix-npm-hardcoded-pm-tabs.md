# Agent Note: Fix npm-hardcoded BYOS and pill switcher

Status: implemented

## Context

Plugin-first BYOS docs (`README.md:18`, `docs/getting-started.md:23`, `docs-web/index.md:55`, `skills/rspfx/SKILL.md:29`) used `npm create vite@latest my-app -- --template react-ts` and `npm i -D @mbsks/rspfx-plugin @mbsks/rspfx-cli` without alternatives; VitePress pill switcher (`docs-web/.vitepress/theme/utils/pmTransform.ts:18`) handled `npm i -g`, `npx skills add`, `--frozen-lockfile`, `tailwind`, `sass`, `pnpm install`, `npm run build` but had no case for `create vite`, `create rsbuild`, or `@mbsks/rspfx-plugin` add, so the BYOS block rendered as plain `npm` code and broke `pnpm`/`yarn`/`bun`/`deno` users; `apps/cli/src/commands/new.ts:22` and `apps/cli/src/cli.ts:166` `--pm` omitted `deno` while docs claimed five managers; `apps/cli/src/commands/migrate.ts:656` hardcoded `pnpm install` hint.

## Decision

Add GH-compatible suffixes and VitePress tabs for BYOS: update `README.md:18`, `docs/getting-started.md:23`, `docs-web/index.md:55`, `skills/rspfx/SKILL.md:29` to `npm create vite@latest my-app -- --template react-ts   # or pnpm create vite@latest / yarn create vite@latest / bun create vite@latest / deno run -A npm:create-vite@latest` + `npm i -D @mbsks/rspfx-plugin @mbsks/rspfx-cli           # or pnpm add -D / yarn add -D / bun add -D / deno add -D`; extend `docs-web/.vitepress/theme/utils/pmTransform.ts:98` to detect `create vite`/`create rsbuild` and `@mbsks/rspfx-plugin` add (single and combined `create`+`cd my-app`+`add` block returning two `pmTag` with `codeBlockHtml` for `cd`) and add `create rsbuild@latest` mapping (`npm`/`pnpm`/`yarn`/`bun`/`deno run -A npm:create-rsbuild@latest`); update global-install lines (`docs/getting-started.md:10`, `skills/rspfx/SKILL.md:19`, `README.md:29`, `docs/migration-from-spfx.md:36`, `docs/upgrading-spfx-version.md:105`, `apps/cli/README.md:10`, `docs-web/public/llm.txt:61`) to `npm i -g @mbsks/rspfx-cli   # or pnpm add -g / yarn global add / bun add -g / deno install -g`; generalize docs mentions (`docs/commands.md:13,31,141`, `docs/architecture.md:106`, `docs/project-structure.md:29`, `docs/why-rspfx.md:80`, `docs/frameworks.md:16`) to list all five managers; fix `skills/rspfx/SKILL.md:53,64,97,105` to `bun install # or pnpm install / npm install / yarn / deno install`, `rspfx dev`, `bun add -D sass # or pnpm add -D sass / npm i -D sass / yarn add -D sass / deno add -D npm:sass`, `bun add @pnp/sp @pnp/graph # or pnpm add / npm i / yarn add / deno add`; add `deno` to `apps/cli/src/commands/new.ts:22` `PACKAGE_MANAGERS` and `apps/cli/src/cli.ts:166` `--pm <pnpm|npm|yarn|bun|deno>`; change `apps/cli/src/commands/migrate.ts:656` hint to `<pm> install (pnpm / npm / yarn / bun / deno)`; update `docs/deployment.md:200` troubleshooting to list all PMs.

## Consequences

BYOS onboarding is now package-manager agnostic on GitHub raw (comment shows all alternatives) and on VitePress (pill switcher `pnpm`/`npm`/`yarn`/`bun`/`deno` tabs for `create vite`, `create rsbuild`, and `add @mbsks/rspfx-plugin`); `rspfx new --pm deno` and `--pm` validation accept `deno`; `rspfx migrate` hint no longer misleads to `pnpm` only; no template or plugin code change required, as starters remain `create-vite`/`create-rsbuild`/`better-t-stack` plus `rspfxVite()`/`rspfxRsbuild()`/`RSpfxPlugin` in `packages/plugin/src/vite.ts`, `rsbuild.ts`, `rspack.ts`.
