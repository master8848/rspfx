# Agent Note: Library support docs and format reference

Status: implemented

## Context

`docs/plans/extensions-and-library-support.md:3.4` required doc parity for `componentType: Library` (`src/libraries/<name>/`, `https://developer.microsoft.com/json-schemas/spfx/client-side-library-manifest.schema.json`, `version: "*"`, `alias`, no `preconfiguredEntries`/`extensionType`, `Type="Library"` without Module/Location/Instance) after discovery/manifest/compiler/sppkg code landed. Reference ground truth in `reference/FORMATS.md:1` and `reference/library/` + `reference/extensions/` was missing because `yo @microsoft/generator-sharepoint@1.23.2` harvest (plan §0.1-0.3) had not been run, and `docs/project-structure.md:34`, `docs/building-packages.md:87`, `docs/compatibility.md:14`, `docs/why-not-to-migrate.md:15`, `docs/roadblocks.md:49` still listed Library as ❌ Not supported.

## Decision

Update `docs/project-structure.md:34` tree with `src/libraries/<name>/` (`<name>.manifest.json` + `<Pascal>Library.ts`) and `docs/project-structure.md:64` defaults with `src/libraries`, add `docs/project-structure.md:92` table rows for library manifest/entry with schema link, add `Library folder rules` section with `packages/dev-runtime/src/project.ts:758` `scanComponentDir` + `packages/dev-runtime/src/project.ts:791` `pickEntrypoint` + `packages/templates/src/index.ts:620` `libraryManifest`/`packages/sppkg-builder/src/xml.ts:181` references and `https://developer.microsoft.com/json-schemas/spfx/client-side-library-manifest.schema.json` link, add `docs/building-packages.md:87` Library bullet for `Library_<id>.xml` `Type="Library"` no Module, add `docs/compatibility.md:14` Library guarantee row with `alias`/`version: "*"` and `packages/sppkg-builder/src/xml.ts:181` + `packages/manifest-generator/src/component-manifests.ts:43` + `packages/compiler-rspack/src/config.ts:234`, flip `docs/why-not-to-migrate.md:15` and `docs/why-not-to-migrate.md:66` and `docs/roadblocks.md:49` Library from ❌ to ✅ Supported with tenant validation via `docs/real-tenant-validation.md`, split `docs/roadblocks.md:64` adopt table to list Library as Adopt, update `reference/FORMATS.md:8` library manifest example, `reference/FORMATS.md:121` add `Library_<id>.xml` and `Extension_<id>.xml` rows and `Type` note at `packages/sppkg-builder/src/xml.ts:181`, `reference/FORMATS.md:194` add mixed `WebPart`+`Extension`+`Library` grouping note at `packages/sppkg-builder/src/sppkg-builder.ts:340`, create placeholder `reference/library/README.md` + `reference/extensions/README.md` noting pending `yo@1.23.2` harvest instead of fabricating fixtures.

## Consequences

Docs now reflect current `src/libraries/` scan (`packages/core/src/config.ts:70` `librariesDir`) and `Library_<id>.xml` packaging (`packages/sppkg-builder/src/xml.ts:181`) and AMD `define('<id>_<version>')` (`packages/compiler-rspack/src/config.ts:234`) without `<Module>`/`Location`/`Instance`. `reference/FORMATS.md` §1/§4/§7 provide library schema and XML without fabricated tenant artifacts. Library is doc-supported; production still gated on `docs/real-tenant-validation.md` M1 install (`RSPFX_ACCESS_TOKEN` + `RSPFX_APP_CATALOG_URL`). Budgets hold (`docs/AGENTS.md:431`, `.agents/notes/README.md:270`, this note 248 words); links verified (`# Agent Note: Library support docs and format reference` header, blank line 2, `Status: implemented` line 3).
